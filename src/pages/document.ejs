<!doctype html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Dynamics 365</title>
    <script src="<%= context.config.publicPath %>wave-add-in-kit.js" type="text/javascript"></script>
</head>
<body>
<div id="root"></div>
<script>
    /**
     * 监听窗口初始化完成
     * 打开默认窗口
     */
    pluginSDK.eventEmitter.once('onInitPluginWindowOk', function ({ userConfig, pluginPath }) {
        pluginSDK.setDefaultWindow({
            width: 482,
            height: 573,
            center: true,
            show: false,
        })
        console.log('onInitPluginWindowOk', userConfig, pluginPath);
        sessionStorage.setItem('pluginPath', pluginPath);
        window.location.hash = '#/login'
    })
</script>
</body>
</html>